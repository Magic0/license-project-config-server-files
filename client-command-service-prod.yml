server:
  port: ${CUSTOM.SERVER.PORT:9045}

# Postgres Configuration
spring:
  jpa:
    databasePlatform:
    hibernate: 
      ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource: 
    url: ${POSTGRESQL.URL.CONNECTION:jdbc:postgresql://localhost:5433/mitocode}
    username: mitocode
    password: '{cipher}adfe9fc5fb8410157309a6f7278029c1e0314c5b34b755fd34809f0cbb195938'

kafka:
  mitocode:
    server: localhost
    topicName: mitocode
    topicName1: mitocode1
    port: 9092