server:
  port: ${CUSTOM.SERVER.PORT:9045}

# Postgres Configuration
spring:
  jpa:
    databasePlatform:
    hibernate: 
      ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource: 
    url: ${POSTGRESQL.URL.CONNECTION:jdbc:postgresql://localhost:5433/mitocode}
    username: mitocode
    password: '{cipher}e88ddb77156db189b0388a44b922ee353f104abd7210efe924baf38bcd2b65ce'

kafka:
  mitocode:
    server: localhost
    topicName: mitocode
    topicName1: mitocode1
    port: 9092